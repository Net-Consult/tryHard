// exercice 1 : trier les hotels par nombre de chambres (plus grand en 1er) et créer un tableau contenant seulement
// le nom des hotels dans leur ordre de tri


/*
class Hostels {

    id;

    name;

    roomNumbers;

    pool;


    rooms;


    constructor(id, name, roomNumbers, pool, ...others) {

        this.name = name;
        this.id = id;
        this.roomNumbers = roomNumbers;
        this.pool = pool;
       this.rooms=others
    }

    sayName() {

        console.log(this.name);
    }

}

let tab=[

 hostel1 = new Hostels(0, 'hotel ocean', 10, 'true', 'roomName','size','id'),


 hostel2 = new Hostels(1, 'hotel des pins', 15, 'true', 'roomName','size','id')
];

hostel1.sayName();

console.log(tab);
*/
